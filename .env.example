# Copie para .env e preencha. NUNCA commite o .env.

# Ambiente ativo dos configs YAML (environments: local | airflow)
ENV=local

# Raiz do data lake local (landing/bronze) e dos seeds do dbt (repo the_dw)
LAKE_ROOT=/media/lucas/Files/2.Projetos/0.mylake
SEEDS_ROOT=/home/lucas/workspace/the_dw/seeds

# Postgres (destino raw.*)
DB_HOST=localhost
DB_PORT=5435
DB_NAME=
DB_USER=
DB_PASSWORD=

# dbt (usado só pelo repo demodadosdw, mantido aqui por conveniência)
DBT_USER=
DBT_PASSWORD=

# Pipeline energia/solar — portal apsystemsema.com
APSYSTEMS_USER=
APSYSTEMS_PASSWORD=

# Pipeline financas/google_finance — service account do Google (arquivo FORA do repo)
GOOGLE_CREDENTIALS_FILE=~/.secrets/finances-py-a58b0d2a733f.json
# URLs das planilhas, uma env var por chave do config.yml (URL_FINANCE_<CHAVE>)
URL_FINANCE_LUCAS_JESSICA=
URL_FINANCE_DEUSA=

# Pluggy (não utilizado no momento)
CLIENT_ID=
CLIENT_SECRET=
